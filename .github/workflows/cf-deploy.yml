name: Deploy Worker
on:
  push:
    branches:
      - main
  pull_request:
  repository_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - name: Build & Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.kMliIbQS_q67qoBZxW0NAEef8sm9KgdL0oujEvnM }}
          accountId: ${{ secrets.2f478d67b08bed19e709dcd93eddf34d }}
